# git

一个用于练习与实验的 Git 仓库。

## 用途

存放小型脚本、实验性代码和学习笔记。不属于任何生产项目。

## 使用

```bash
git clone https://github.com/<your-username>/git.git
cd git
```

## 目录结构

```
.
├── README.md
└── .gitignore
```

## 约定

- 主分支为 `main`。
- 提交信息使用祈使句（如 `Add sensor parsing script`），首行不超过 72 字符。
- 不提交密钥、令牌、数据文件与本地环境目录。
